To start from scratch:

Make sure memoria_db is okay or setted up in phpmyadmin, the sql.text

config.php set it up, the root and password, the everything

.htaccess - The RewriteBase / and everything, basically if its in root folder or something right -if something is 404 or missing or shits, maybe this is the problem!!!!????

the notallowed.php 404 html load it change it if its not in the roootest root folder lol

changes soon I guess:

auth.php -   setcookie('auth_token', $jwt, [
            'expires' => time() + $JWT_EXPIRATION,
            'path' => '/',
            'secure' => false, // set true in HTTPS (production)
            'httponly' => true,
            'samesite' => 'Strict'
        ]);

Reasonninng i guesss:
if the backend and frontend will really be separate
the folder api/ is okay since if they are separated, the api has its own index.html like a docs so in it the roofolder/api is okay i guess please sabta

textbee 
- change textbee.php if you change to another SMS API, 
- the database column as well
- currently used in the users.php for sending notification


things I saw:
accounts.html 
- the searching, make local first then search from the api
- the pagination i guess

logs.html 
- not needed pagination i guess since it is all and people can wait.
- also its just to tiring

ratelimit - must be fixed i guess....

# Limit request body to 10MB to prevent lag from massive files idk??? APACHE SERVER LIMIT. .htacess
# LimitRequestBody 10485760
????

Blocks & Graves = The physical map. (They never expire).

Deceased & Contacts = The biological people. (They never expire).

Interments = The 5-year event linking the Person to the Grave. (This is the ONLY thing that expires).
LEASES WILL expire

RECOMMENDATIONS / SOON FUTURE I GUESS:
- Add photo/scan documents and attach it to the file or something...


JS/ FRONTEND:
ACCOUNT.HTML - ROLE: Administrator, Office Staff, Grounds Staff CHANGE TO HERE PLEASE

Blocks:
display in the map: api/blocks - returns all blocks
when clicked api/blocks/[id] - shows all the graves

If changing database
users.php - the roles and statuses are in constant
blocks.php - roles as well

Co interments - in the records.html + or we can merge it using IDs


HUHUHU TO BE SOON I GUESS:

reports - no payments included because its just for simple ledger and not counted huhuhuhuhuhuhu no payments statistics

map
reserve
monitor
records
YAWA KA LISOD HUHUHUHUHUHUHUHUHU

first, will they even use this? regardless, papasara lang unta mi sir huhu bahala nana sila hahauhuhuhuhuhuhuhhuhu
this is pure our own logic i guess. i hope we are fine soon.... tabanggg